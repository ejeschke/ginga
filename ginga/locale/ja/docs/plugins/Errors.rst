``Errors`` プラグインは、ビューアにエラーメッセージを報告します。

**プラグインの種類: グローバル**

``Errors`` はグローバルプラグインです。開けるインスタンスは 1 つだけです。

**使い方**

Ginga でエラーが発生すると、そのメッセージがここに報告されることがあります。

このプラグインは通常、閉じられるようには構成されていませんが、設定ファイルで
「closeable」設定を True にすると閉じられるようになります。その場合、UI の
下部に「閉じる」と「ヘルプ」のボタンが追加されます。
