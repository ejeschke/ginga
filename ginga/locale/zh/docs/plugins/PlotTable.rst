用于为表格中任意两个选定列显示基本绘图的插件。

**插件类型：本地**

``PlotTable`` 是本地插件，这意味着它与某个通道相关联。
它不是单例，这意味着可以为每个通道打开多个实例。

**用法**

``PlotTable`` 是一个插件，旨在为给定的 FITS 表格 HDU（可通过 ``MultiDim``
访问）绘制任意两个选定列。
对于被掩码的列，不会显示被掩码的数据（即使 ``(X, Y)`` 对中只有一个被掩码）。
它旨在快速查看表格数据，而非用于详细的科学分析。
