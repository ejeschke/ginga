相机模式启用可在使用 OpenGL 后端的 Ginga 图像查看器中操作相机的按键分配。

.. note:: 除非查看器使用 OpenGL 后端，否则相机模式无法工作。

进入模式的方式
--------------
* 空格，然后「c」

退出模式的方式
--------------
* Esc

模式的默认按键分配
------------------
* s : 保存当前相机设置以便稍后恢复
* r : 恢复为已保存的相机设置
* 3 : 将相机视图切换为 3D 模式
* 滚动 : 跟踪相机（推近和拉远）
* 左键拖动 : 用相机环绕视图
* 右键拖动 : 用相机平移视图
