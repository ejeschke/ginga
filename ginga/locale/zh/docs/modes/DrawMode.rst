绘制模式启用便于绘制的按键分配。

进入模式的方式
--------------
* 空格，然后「b」

退出模式的方式
--------------
* Esc

模式的默认按键分配
------------------
* v : 在绘制或编辑画布时向多边形添加顶点
* z : 在绘制或编辑画布时从多边形删除顶点
* x : 在编辑画布时删除选定的对象
* 左键拖动 : 以定义的形状在画布上绘制
* 右键拖动 : 以定义的形状在画布上绘制
