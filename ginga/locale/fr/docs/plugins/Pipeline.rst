Plugin simple de pipeline de traitement de données pour Ginga.

**Type de plugin : Local**

``Pipeline`` est un plugin local, ce qui signifie qu'il est associé à un canal.
Une instance peut être ouverte pour chaque canal.

**Utilisation**
